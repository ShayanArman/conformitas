# Compliance Agent
Make sure you read all the code before answering. Don't guess, check relevant code files before answering.

# Instructions
We can always improve upon the code.

# Never Do
Never, ever make commits.

# No Data Model

# Project Notes
conformitas.xyz is a regulatory and compliance search engine for construction companies.

# No Database

# **important Focus on Flow
The main architecture is a Flow.
Focus on the Flow.
The Flow is:
User
-> server/trpc/router/compliance/
-> server/agents/ComplianceAgent
-> server/services/exa

# Always finish the Task I told you. Then run this prompt AGAIN
`is that a good plan now that we have it? could we make that plan even simpler without adding or keeping track of any data?`
Finally run one final pass and simplify!!
for example: `okay now look over all of the components made and simplify! I’m doing one more cleanup pass: removing unnecessary useMemo usage for simple booleans and tightening imports.`